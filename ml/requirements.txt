# IndustrialBrain — ML Layer Dependencies
# CPU-only build: no CUDA, no GPU packages

# Document parsing
pdfplumber==0.10.3
pymupdf==1.23.8
python-docx==1.1.0
openpyxl==3.1.2
pandas==2.1.4
extract-msg==0.48.0

# OCR (PaddleOCR for CPU, pytesseract as fallback)
paddlepaddle==2.6.1
paddleocr==2.7.3
# pytesseract==0.3.10       # uncomment if PaddleOCR install fails on Windows

# Image processing
opencv-python-headless==4.9.0.80
Pillow==10.2.0
numpy==1.26.3

# NLP / NER
spacy==3.7.2
# en_core_web_sm installed separately:
#   python -m spacy download en_core_web_sm

# P&ID Symbol Detection
ultralytics==8.1.0

# ML — predictive maintenance (CPU scikit-learn)
scikit-learn==1.4.0
joblib==1.3.2
scipy==1.12.0

# Embeddings (used by both ML + backend)
sentence-transformers==2.3.1

# Vector DB client
chromadb==0.4.22

# Database clients
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
neo4j==5.17.0
redis==5.0.1

# Report generation
reportlab==4.1.0
jinja2==3.1.3

# Utilities
python-dotenv==1.0.0
pydantic==2.5.3
tqdm==4.66.1
loguru==0.7.2

# Kaggle (for dataset download in Colab notebooks)
kaggle==1.6.3
