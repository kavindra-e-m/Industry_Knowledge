#!/usr/bin/env bash
set -e

ROOT_DIR="$(cd "$(dirname "$0")/../.." && pwd)"
cd "$ROOT_DIR"

echo "Resetting demo environment to a clean state..."
echo "This placeholder script can be extended to reset databases and re-seed demo content."
